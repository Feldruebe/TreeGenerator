<Window x:Class="TreeGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TreeGenerator"
        xmlns:viewmodel="clr-namespace:TreeGenerator.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"
        DataContext="{Binding Source={StaticResource Locator}, Path=Main}"  >
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center">
      <Border BorderThickness="2" BorderBrush="Red" HorizontalAlignment="Center">
        <Image RenderOptions.BitmapScalingMode="NearestNeighbor" Source="{Binding ImageSkeletton}"/>
      </Border>
      <Border BorderThickness="2" BorderBrush="Blue" HorizontalAlignment="Center">
        <Image RenderOptions.BitmapScalingMode="NearestNeighbor" Source="{Binding ImageTree}"/>
      </Border>
    </StackPanel>
    <StackPanel Grid.Row="1">
      <StackPanel Orientation="Horizontal">
        <TextBlock Margin="5,5,10,5">Image Width</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding ImageWidth}" />

        <TextBlock Margin="5,5,10,5">Image Height</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding ImageHeight}" />
      </StackPanel>
      <StackPanel Orientation="Horizontal">
        <TextBlock Margin="5,5,10,5">Trunk Size</TextBlock>
        <TextBox Text="{Binding TreeTrunkSize}" MinWidth="50" Margin="5,5,10,5" />
        <Slider Value="{Binding TreeTrunkSize}" MinWidth="100" Maximum="{Binding ImageHeight}" Minimum="1" Margin="5,5,10,5" />
      </StackPanel>
      <StackPanel Orientation="Horizontal">
        <TextBlock Margin="5,5,10,5">Skew Angle</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding SkewAngle}" />
        <Slider Value="{Binding SkewAngle}" MinWidth="100" Maximum="80" Minimum="-80" Margin="5,5,10,5" />
        <TextBlock Margin="5,5,10,5">Start</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding SkewAngleStart}" />
        <Slider Value="{Binding SkewAngleStart}" MinWidth="100" Maximum="{Binding TreeTrunkSize}" Minimum="0" SmallChange="1" Margin="5,5,10,5" IsSnapToTickEnabled="True" />
      </StackPanel>
      <StackPanel Orientation="Horizontal">
        <TextBlock Margin="5,5,10,5">Rotation Angle</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding RotationAngle}" />
        <Slider Value="{Binding RotationAngle}" MinWidth="100" Maximum="240" Minimum="-240" Margin="5,5,10,5" />
        <TextBlock Margin="5,5,10,5">Start</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding RotationAngleStart}" />
        <Slider Value="{Binding RotationAngleStart}" MinWidth="100" Maximum="{Binding TreeTrunkSize}" Minimum="0" SmallChange="1" Margin="5,5,10,5" IsSnapToTickEnabled="True" />
      </StackPanel>
      <StackPanel Orientation="Horizontal">
        <TextBlock Margin="5,5,10,5">Branch Count</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding BranchCount}" />
        <Slider Value="{Binding BranchCount}" MinWidth="100" Maximum="100" Minimum="0" SmallChange="1" Margin="5,5,10,5" IsSnapToTickEnabled="True" />
        <TextBlock Margin="5,5,10,5">Branch Start</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding BranchStart}" />
        <Slider Value="{Binding BranchStart}" MinWidth="100" Maximum="{Binding TreeTrunkSize}" Minimum="0" SmallChange="1" Margin="5,5,10,5" IsSnapToTickEnabled="True" />
      </StackPanel>
      <StackPanel Orientation="Horizontal">
        <TextBlock Margin="5,5,10,5">Branch Min Length</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding BranchLengthMin}" />
        <TextBlock Margin="5,5,10,5">Branch Max Length</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding BranchLengthMax}" />
      </StackPanel>
      <StackPanel Orientation="Horizontal">
        <TextBlock Margin="5,5,10,5">Branch Distance</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding BranchDistance}" />
        <Slider Value="{Binding BranchDistance}" MinWidth="100" Maximum="{Binding TreeTrunkSize}" Minimum="1" SmallChange="1" Margin="5,5,10,5" IsSnapToTickEnabled="True" />
      </StackPanel>
      <StackPanel Orientation="Horizontal">
        <TextBlock Margin="5,5,10,5">Branch Skew</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding BranchSkew}" />
        <Slider Value="{Binding BranchSkew}" MinWidth="100" Maximum="120" Minimum="10" SmallChange="1" Margin="5,5,10,5" IsSnapToTickEnabled="True" />
        <TextBlock Margin="5,5,10,5">Skew Deviation</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding BranchSkewDeviation}" />
        <Slider Value="{Binding BranchSkewDeviation}" MinWidth="100" Maximum="50" Minimum="0" SmallChange="1" Margin="5,5,10,5" IsSnapToTickEnabled="True" />
      </StackPanel>

      <StackPanel Orientation="Horizontal">
        <TextBlock Margin="5,5,10,5">Branch Rotation</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding BranchRotationAngle}" />
        <Slider Value="{Binding BranchRotationAngle}" MinWidth="100" Maximum="240" Minimum="-240" SmallChange="1" Margin="5,5,10,5" IsSnapToTickEnabled="True" />
        <TextBlock Margin="5,5,10,5">Rotation Start</TextBlock>
        <TextBox Margin="5,5,10,5" MinWidth="50" Text="{Binding BranchRotationAngleStart}" />
        <Slider Value="{Binding BranchRotationAngleStart}" MinWidth="100" Maximum="{Binding BranchLengthMax}" Minimum="0" SmallChange="1" Margin="5,5,10,5" IsSnapToTickEnabled="True" />
      </StackPanel>
    </StackPanel>
    <Button Grid.Row="2" Click="Button_Click">Generate</Button>
  </Grid>
</Window>
